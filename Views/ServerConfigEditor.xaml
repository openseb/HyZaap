<UserControl x:Class="HyZaap.Views.ServerConfigEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HyZaap.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="{StaticResource HytaleDarkBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource HytaleCardBg}" Padding="20" BorderBrush="{StaticResource HytaleBorder}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="← Back" Padding="10,8" Background="{StaticResource HytaleBorder}"
                       Margin="0,0,20,0" Command="{Binding CancelCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Background" Value="{StaticResource HytaleBorder}"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#2D2D35"/>
                                    <Setter Property="Foreground" Value="#707070"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4A4A55"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Server.Name}" FontSize="24" FontWeight="Bold" 
                              Foreground="{StaticResource HytaleText}"/>
                    <TextBlock Text=" - Config Editor" FontSize="24" FontWeight="Bold" 
                              Foreground="{StaticResource HytaleText}" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Config Editor -->
        <Border Grid.Row="1" Background="{StaticResource HytaleCardBg}" BorderBrush="{StaticResource HytaleBorder}" 
                BorderThickness="1" Margin="20" Padding="10" CornerRadius="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox Text="{Binding ConfigJson, UpdateSourceTrigger=PropertyChanged}" 
                        FontFamily="Consolas" FontSize="12" 
                        AcceptsReturn="True" AcceptsTab="True"
                        TextWrapping="NoWrap"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto"
                        Background="#0F0F12"
                        Foreground="{StaticResource HytaleText}"
                        BorderThickness="0"/>
            </ScrollViewer>
        </Border>

        <!-- Status Message -->
        <Border Grid.Row="2" Background="{StaticResource HytaleCardBg}" Padding="15" Margin="20,0,20,0" 
                CornerRadius="4" BorderBrush="{StaticResource HytaleBorder}" BorderThickness="1"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource HytaleText}" 
                      TextWrapping="Wrap" MinHeight="20"/>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="{StaticResource HytaleCardBg}" Padding="20" BorderBrush="{StaticResource HytaleBorder}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,0,10,0" Width="100"/>
                    <Button Content="Save &amp; Restart" Command="{Binding SaveAndRestartCommand}" 
                           Background="{StaticResource HytaleAccentGreen}" Width="150"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

